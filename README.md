# sportscam

Create a python venv with `python -m venv venv`

Activate with `venv\scripts\activate`
Install with `pip install -r requirements.txt`

# BUGS
* Camera cuts don't work across file boundaries. 

# Future features
* Zoom